# ==========================================
# macOS (Darwin) Specific
# ==========================================
.DS_Store
.AppleDouble
.LSOverride

# Icon must end with two carriage returns
Icon?

# Thumbnails and filesystem metadata
._*
.Spotlight-V100
.Trashes

# ==========================================
# Windows 11 Specific
# ==========================================
# Windows image thumbnails
Thumbs.db
Thumbs.db:encrypt
ehthumbs.db
ehthumbs_vista.db

# Folder layout and desktop configurations
Desktop.ini
$RECYCLE.BIN/

# Windows shortcuts
*.lnk

# ==========================================
# Linux / RHEL Specific
# ==========================================
# Temporary files generated by text editors or tools
*~

# KDE/GNOME folder settings
.directory

# Ubuntu/Debian/RedHat crash logs
*.crash

# ==========================================
# Common Editor & IDE Artifacts (Cross-Platform)
# ==========================================
# VS Code settings (per-project overrides you usually don't want to force on others)
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json

# Vim/Neovim swap, backup, and undo files
[._]*.s[a-v][a-z]
[._]*.sw[a-p]
[._]s[a-v][a-z]
[._]sw[a-p]
*.un~
Session.vim
.netrwhist

# JetBrains (IntelliJ, RubyMine, WebStorm, etc.)
.idea/

# Logs and databases (Generic safety nets)
*.log
*.sqlite
